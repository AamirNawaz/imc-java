server:
  servlet:
    context-path: "/api"
  port: 9000

spring:
  application:
    name: "imc-java"

#Just for demo purpose
  security:
    user:
      name: aamir
      password: aamir
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
      ddl-auto: none
    show-sql: true



  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/imc-database}
    username: ${DATABASE_USERNAME:aamirnawaz}
    password: ${DATABASE_PASSWORD:aamirnawaz}

  servlet:
      multipart:
        max-file-size: 10MB
        max-request-size: 10MB

